# Drop-in for journald to limit log storage on Pi 3 (limited SD card space).
# Install: sudo cp journald-atss.conf /etc/systemd/journald.conf.d/atss.conf
#          sudo systemctl restart systemd-journald

[Journal]
SystemMaxUse=50M
SystemMaxFileSize=10M
MaxRetentionSec=7day
Compress=yes
